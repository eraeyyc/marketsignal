{
  "enabledPlugins": {
    "claude-code-setup@claude-plugins-official": true,
    "claude-md-management@claude-plugins-official": true
  },
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "if": "Bash(git push*)",
            "command": "/Users/MAC/projects/marketsignal/.claude/hooks/deploy.sh",
            "statusMessage": "Deploying to VPS...",
            "timeout": 60
          }
        ]
      },
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "input=$(cat); file=$(echo \"$input\" | python3 -c \"import sys,json; d=json.load(sys.stdin); print(d.get('tool_input',{}).get('file_path',''))\" 2>/dev/null); if [[ \"$file\" == *.py ]]; then python3 -m py_compile \"$file\" 2>&1 && echo \"[syntax ok] $file\" || echo \"[SYNTAX ERROR] $file\"; fi"
          }
        ]
      }
    ]
  }
}
